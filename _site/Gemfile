source "https://rubygems.org"

# Match GitHub Pages’ build exactly (includes jekyll, jekyll-seo-tag, feed, sitemap, jemoji, etc.)
gem "github-pages", group: :jekyll_plugins

# Classic pagination (provides `paginator.posts`)
gem "jekyll-paginate"

# Ruby 3+ conveniences
gem "webrick"
gem "csv"
